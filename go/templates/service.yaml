apiVersion: v1
kind: Service
metadata:
  name: {{ include "go.fullname" . }}
  labels:
    app: {{ include "go.fullname" . }}
spec:
  selector:
    app: {{ include "go.fullname" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: {{ .Values.service.type }}
