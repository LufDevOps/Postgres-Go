apiVersion: v1
kind: Service
metadata:
  name: {{ include "metric-chart.fullname" . }}
  labels:
    {{- include "metric-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metric-chart.selectorLabels" . | nindent 4 }}
