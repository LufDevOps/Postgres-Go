apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metric-chart.fullname" . }}-test-connection"
  labels:
    {{- include "metric-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metric-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
